{{- /* +argocd:skip-file-rendering — root-app 훑기 제외. 렌더 출력에는 남지 않는다 */ -}}
apiVersion: karpenter.sh/v1
kind: NodePool
metadata:
  name: default
  annotations:
    kubernetes.io/description: >-
      플랫폼 baseline NodePool. 시스템·컨트롤러는 managed NG 에 두고 앱·버스트만 여기서 받는다
      (20 열린 항목 2). Karpenter 컨트롤러 자신은 chart affinity 가
      karpenter.sh/nodepool DoesNotExist 라 이 풀을 자연히 회피한다.
spec:
  template:
    spec:
      requirements:
        - key: kubernetes.io/arch
          operator: In
          values: {{ toYaml .Values.architectures | nindent 12 }}
        - key: kubernetes.io/os
          operator: In
          values: ["linux"]
        - key: karpenter.sh/capacity-type
          operator: In
          values: {{ toYaml .Values.capacityTypes | nindent 12 }}
        - key: karpenter.k8s.aws/instance-category
          operator: In
          values: {{ toYaml .Values.instanceCategories | nindent 12 }}
        - key: karpenter.k8s.aws/instance-generation
          operator: Gt
          values: [{{ .Values.instanceGenerationGt | quote }}]
      nodeClassRef:
        group: karpenter.k8s.aws
        kind: EC2NodeClass
        name: default
  limits:
    cpu: {{ .Values.limits.cpu | quote }}
  disruption:
    consolidationPolicy: {{ .Values.disruption.consolidationPolicy }}
    consolidateAfter: {{ .Values.disruption.consolidateAfter }}
